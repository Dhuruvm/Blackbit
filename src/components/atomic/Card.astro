---
interface Props {
  class?: string;
  hover?: boolean;
}

const { class: className = '', hover = false } = Astro.props;
const classes = `card ${hover ? 'card-hover' : ''} ${className}`;
---

<div class={classes}>
  <slot />
</div>
